# MySQL root password (used by both mysql and backend containers)
MYSQL_ROOT_PASSWORD=your_password_here

# JWT secret key for token signing (change to a random string)
JWT_SECRET_KEY=your_random_secret_here
