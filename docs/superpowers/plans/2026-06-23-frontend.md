# 博客前端 (Nuxt 3) 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 构建 YeYi 博客的 Nuxt 3 前端，使用洛克魔法书设计风格，对接后端 API 展示文章、评论、搜索等功能。

**Architecture:** Nuxt 3 SSR 应用，使用 Composition API + `<script setup>`，通过 `useFetch` / `$fetch` 调用后端 API。Tailwind CSS + 自定义 Rocom 设计系统变量。

**Tech Stack:** Nuxt 3, Vue 3, Tailwind CSS, @nuxtjs/color-mode, markdown-it, KaTeX, Prism.js

## Global Constraints

- Nuxt 3 + Vue 3 Composition API（`<script setup>`）
- Tailwind CSS 做样式，Rocom 设计 token 通过 CSS 变量定义
- 所有 API 调用通过 Nuxt 的 `useFetch` / `$fetch`，base URL 通过 runtime config 配置
- 暗黑模式通过 `@nuxtjs/color-mode` 实现
- 响应式设计：mobile-first，断点 sm/md/lg/xl
- 代码风格：无注释除非必要，组件名 PascalCase
- Alimama FangYuanTi 字体通过 `@font-face` 加载

---

## 文件结构

```
frontend/
├── nuxt.config.ts
├── app.vue
├── tailwind.config.ts
├── assets/
│   ├── css/
│   │   ├── main.css              # 全局样式 + Rocom CSS 变量
│   │   └── typography.css        # 文章排版样式
│   └── fonts/
│       ├── AlimamaFangYuanTiVF-Thin.ttf
│       ├── AlimamaFangYuanTiVF-Thin.woff2
│       └── AlimamaFangYuanTiVF-Thin.woff
├── components/
│   ├── AppHeader.vue             # 顶部导航栏
│   ├── AppFooter.vue             # 页脚
│   ├── PostCard.vue              # 文章卡片（首页列表用）
│   ├── PostMeta.vue              # 文章元信息（日期、分类、标签）
│   ├── CommentSection.vue        # 评论区（列表 + 表单）
│   ├── CommentItem.vue           # 单条评论
│   ├── CommentForm.vue           # 评论表单
│   ├── TagCloud.vue              # 标签云
│   ├── CategoryList.vue          # 分类列表
│   ├── SearchBox.vue             # 搜索框
│   ├── TableOfContents.vue       # 文章目录（TOC）
│   ├── AnnouncementBar.vue       # 公告栏
│   ├── Pagination.vue            # 分页组件
│   ├── DarkToggle.vue            # 暗黑模式切换
│   └── BackToTop.vue             # 回到顶部
├── composables/
│   ├── useApi.ts                 # API 调用封装
│   └── useSiteConfig.ts          # 站点配置（全局）
├── layouts/
│   └── default.vue               # 默认布局
├── pages/
│   ├── index.vue                 # 首页
│   ├── posts/
│   │   └── [slug].vue            # 文章详情
│   ├── categories.vue            # 分类页
│   ├── tags.vue                  # 标签页
│   ├── archive.vue               # 归档页
│   ├── search.vue                # 搜索结果页
│   └── about.vue                 # 关于页
├── plugins/
│   └── markdown.ts               # markdown-it 插件
├── public/
│   └── favicon.ico
└── server/
    └── routes/
        └── rss.xml.ts            # RSS 代理（可选，或直接用后端）
```

---

## Task 1: 项目初始化

**Files:**
- Create: `frontend/nuxt.config.ts`
- Create: `frontend/tailwind.config.ts`
- Create: `frontend/assets/css/main.css`
- Create: `frontend/app.vue`

**Interfaces:**
- Produces: 可运行的 Nuxt 3 项目，Tailwind CSS 配置完成，Rocom CSS 变量定义完毕

- [ ] **Step 1: 初始化 Nuxt 项目**

Run: `npx nuxi@latest init frontend --no-install`
Expected: 生成 frontend/ 目录

- [ ] **Step 2: 安装依赖**

```bash
cd frontend
npm install
npm install -D @nuxtjs/tailwindcss @nuxtjs/color-mode @nuxtjs/google-fonts
npm install markdown-it katex prismjs
npm install -D @types/markdown-it @types/prismjs
```

- [ ] **Step 3: 创建 nuxt.config.ts**

```typescript
export default defineNuxtConfig({
  devtools: { enabled: true },
  modules: ['@nuxtjs/tailwindcss', '@nuxtjs/color-mode'],
  colorMode: {
    classSuffix: '',
    preference: 'light',
    fallback: 'light',
  },
  runtimeConfig: {
    public: {
      apiBase: process.env.NUXT_PUBLIC_API_BASE || 'http://localhost:8000',
    },
  },
  app: {
    head: {
      title: 'YeYi 的博客',
      meta: [
        { name: 'description', content: '记录生活与代码' },
        { charset: 'utf-8' },
        { name: 'viewport', content: 'width=device-width, initial-scale=1' },
      ],
      link: [
        { rel: 'icon', type: 'image/x-icon', href: '/favicon.ico' },
      ],
    },
  },
  css: ['~/assets/css/main.css'],
  compatibilityDate: '2024-07-01',
})
```

- [ ] **Step 4: 创建 tailwind.config.ts**

```typescript
import type { Config } from 'tailwindcss'

export default <Config>{
  darkMode: 'class',
  content: [
    './components/**/*.{vue,js,ts}',
    './layouts/**/*.vue',
    './pages/**/*.vue',
    './app.vue',
  ],
  theme: {
    extend: {
      fontFamily: {
        brand: ['"Alimama FangYuanTi"', '"Microsoft YaHei"', '"PingFang SC"', 'sans-serif'],
        mono: ['ui-monospace', 'SFMono-Regular', 'Menlo', 'Consolas', 'monospace'],
      },
      colors: {
        rocom: {
          bg: 'var(--rocom-bg)',
          'bg-warm': 'var(--rocom-bg-warm)',
          'bg-paper': 'var(--rocom-bg-paper)',
          'bg-parchment': 'var(--rocom-bg-parchment)',
          surface: 'var(--rocom-surface)',
          'surface-paper': 'var(--rocom-surface-paper)',
          'surface-strong': 'var(--rocom-surface-strong)',
          'surface-muted': 'var(--rocom-surface-muted)',
          text: 'var(--rocom-text)',
          'text-strong': 'var(--rocom-text-strong)',
          'text-secondary': 'var(--rocom-text-secondary)',
          'text-muted': 'var(--rocom-text-muted)',
          'text-caption': 'var(--rocom-text-caption)',
          'text-disabled': 'var(--rocom-text-disabled)',
          primary: 'var(--rocom-primary)',
          'primary-soft': 'var(--rocom-primary-soft)',
          'primary-strong': 'var(--rocom-primary-strong)',
          'primary-outline': 'var(--rocom-primary-outline)',
          'accent-orange': 'var(--rocom-accent-orange)',
          'accent-blue': 'var(--rocom-accent-blue)',
          outline: 'var(--rocom-outline)',
          separator: 'var(--rocom-separator)',
          control: 'var(--rocom-control)',
          'control-strong': 'var(--rocom-control-strong)',
          'control-hover': 'var(--rocom-control-hover)',
          'nav-surface': 'var(--rocom-nav-surface)',
        },
      },
      borderRadius: {
        xs: '2px',
        sm: '4px',
        md: '6px',
        lg: '8px',
        xl: '10px',
        '2xl': '12px',
        pill: '999px',
      },
      boxShadow: {
        sm: '0 2px 6px rgba(0,0,0,0.08)',
        paper: '0 10px 26px rgba(83,58,35,0.08)',
        lg: '0 14px 32px rgba(75,54,29,0.10)',
        float: '0 26px 58px rgba(76,55,32,0.18)',
      },
    },
  },
  plugins: [],
}
```

- [ ] **Step 5: 创建 assets/css/main.css**

```css
@font-face {
  font-family: "Alimama FangYuanTi";
  src: url("~/assets/fonts/AlimamaFangYuanTiVF-Thin.woff2") format("woff2"),
       url("~/assets/fonts/AlimamaFangYuanTiVF-Thin.woff") format("woff"),
       url("~/assets/fonts/AlimamaFangYuanTiVF-Thin.ttf") format("truetype");
  font-style: normal;
  font-weight: 100 900;
  font-display: swap;
}

:root {
  --rocom-bg: #FFF6E0;
  --rocom-bg-warm: #FAF6ED;
  --rocom-bg-paper: #F4ECDC;
  --rocom-bg-parchment: #EFE6D8;
  --rocom-surface: #FFFFFFDB;
  --rocom-surface-paper: #FAF7EEE0;
  --rocom-surface-strong: #FFFCF7;
  --rocom-surface-muted: #F9F7F2;
  --rocom-text: #3D3528;
  --rocom-text-strong: #1A1510;
  --rocom-text-secondary: #7A7060;
  --rocom-text-muted: #8B735B;
  --rocom-text-caption: #9E8E76;
  --rocom-text-disabled: #C8C0B0;
  --rocom-primary: #F5BC00;
  --rocom-primary-soft: #FFE8A3;
  --rocom-primary-strong: #FFD460;
  --rocom-primary-outline: #C89400;
  --rocom-accent-orange: #D57F24;
  --rocom-accent-blue: #2F6FDA;
  --rocom-success: #388E3C;
  --rocom-warning: #F57C00;
  --rocom-danger: #D32F2F;
  --rocom-info: #60A5FA;
  --rocom-outline: #EDE9DF;
  --rocom-separator: #8D6A4447;
  --rocom-control: #FFFFFF9E;
  --rocom-control-strong: #FFFFFFC7;
  --rocom-control-hover: #FFFFFFEB;
  --rocom-nav-surface: #FFFCF7EB;
  --shadow-sm: 0 2px 6px rgba(0,0,0,0.08);
  --shadow-paper: 0 10px 26px rgba(83,58,35,0.08);
  --shadow-lg: 0 14px 32px rgba(75,54,29,0.10);
  --shadow-float: 0 26px 58px rgba(76,55,32,0.18);
}

.dark {
  --rocom-bg: #17120F;
  --rocom-bg-warm: #1E1A16;
  --rocom-bg-paper: #2A241E;
  --rocom-bg-parchment: #342C23;
  --rocom-surface: #322A1EDB;
  --rocom-surface-paper: #2B241BEB;
  --rocom-surface-strong: #3A3024;
  --rocom-surface-muted: #3A3024C7;
  --rocom-text: #F4EEE1;
  --rocom-text-strong: #FFF8E8;
  --rocom-text-secondary: #D2C2A8;
  --rocom-text-muted: #A8A69F;
  --rocom-text-caption: #918A7C;
  --rocom-text-disabled: #6F685E;
  --rocom-primary: #FFD460;
  --rocom-primary-soft: #FFD46033;
  --rocom-primary-strong: #FFE39A;
  --rocom-primary-outline: #FFD460;
  --rocom-accent-orange: #F6A14A;
  --rocom-accent-blue: #93C5FD;
  --rocom-success: #86EFAC;
  --rocom-warning: #FBBF24;
  --rocom-danger: #FCA5A5;
  --rocom-info: #93C5FD;
  --rocom-outline: #F4EEE129;
  --rocom-separator: #F4EEE124;
  --rocom-control: #3A3024A3;
  --rocom-control-strong: #433729DB;
  --rocom-control-hover: #524331EB;
  --rocom-nav-surface: #221C15F0;
  --shadow-sm: 0 2px 8px rgba(0,0,0,0.22);
  --shadow-paper: 0 14px 32px rgba(0,0,0,0.24);
  --shadow-lg: 0 18px 42px rgba(0,0,0,0.32);
  --shadow-float: 0 28px 64px rgba(0,0,0,0.42);
}

body {
  font-family: "Alimama FangYuanTi", "Microsoft YaHei", "PingFang SC", sans-serif;
  background-color: var(--rocom-bg);
  color: var(--rocom-text);
  transition: background-color 0.3s, color 0.3s;
}

@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    scroll-behavior: auto !important;
    transition-duration: 0.01ms !important;
    animation-duration: 0.01ms !important;
  }
}
```

- [ ] **Step 6: 创建 app.vue**

```vue
<template>
  <NuxtLayout>
    <NuxtPage />
  </NuxtLayout>
</template>
```

- [ ] **Step 7: 验证项目启动**

Run: `cd frontend && npm run dev`
Expected: 开发服务器启动在 http://localhost:3000

- [ ] **Step 8: Commit**

```bash
cd frontend && git add -A && git commit -m "feat: nuxt3 project init with tailwind and rocom design tokens"
```

---

## Task 2: API 封装与站点配置

**Files:**
- Create: `frontend/composables/useApi.ts`
- Create: `frontend/composables/useSiteConfig.ts`

**Interfaces:**
- Produces:
  - `useApi()` — 封装 `$fetch`，自动加 base URL
  - `useSiteConfig()` — 获取并缓存站点配置

- [ ] **Step 1: 创建 useApi.ts**

```typescript
export function useApi() {
  const config = useRuntimeConfig()
  const baseURL = config.public.apiBase

  return {
    get: <T>(url: string, params?: Record<string, any>) =>
      $fetch<T>(url, { baseURL, params }),
    post: <T>(url: string, body?: any) =>
      $fetch<T>(url, { baseURL, method: 'POST', body }),
    put: <T>(url: string, body?: any) =>
      $fetch<T>(url, { baseURL, method: 'PUT', body }),
    del: <T>(url: string) =>
      $fetch<T>(url, { baseURL, method: 'DELETE' }),
  }
}
```

- [ ] **Step 2: 创建 useSiteConfig.ts**

```typescript
interface SiteConfig {
  site_title: string
  site_subtitle: string
  logo_url: string
  favicon_url: string
  footer_text: string
  social_links: Record<string, string>
  comment_enabled: boolean
  comment_need_review: boolean
}

const siteConfig = ref<SiteConfig | null>(null)

export function useSiteConfig() {
  const api = useApi()

  const fetchConfig = async () => {
    if (!siteConfig.value) {
      siteConfig.value = await api.get<SiteConfig>('/api/v1/site/config')
    }
    return siteConfig.value
  }

  return { siteConfig, fetchConfig }
}
```

- [ ] **Step 3: Commit**

```bash
cd frontend && git add -A && git commit -m "feat: API composable and site config composable"
```

---

## Task 3: 布局与导航组件

**Files:**
- Create: `frontend/layouts/default.vue`
- Create: `frontend/components/AppHeader.vue`
- Create: `frontend/components/AppFooter.vue`
- Create: `frontend/components/DarkToggle.vue`
- Create: `frontend/components/SearchBox.vue`

**Interfaces:**
- Produces: 默认布局，顶部导航栏（Logo、导航链接、搜索框、暗黑切换），页脚

- [ ] **Step 1: 创建 default.vue**

```vue
<script setup lang="ts">
const { fetchConfig } = useSiteConfig()
await fetchConfig()
</script>

<template>
  <div class="min-h-screen flex flex-col bg-rocom-bg">
    <AppHeader />
    <main class="flex-1 w-full max-w-4xl mx-auto px-4 py-8">
      <slot />
    </main>
    <AppFooter />
  </div>
</template>
```

- [ ] **Step 2: 创建 AppHeader.vue**

```vue
<script setup lang="ts">
const { siteConfig } = useSiteConfig()
const route = useRoute()
const mobileMenuOpen = ref(false)

const navLinks = [
  { path: '/', label: '首页' },
  { path: '/categories', label: '分类' },
  { path: '/tags', label: '标签' },
  { path: '/archive', label: '归档' },
  { path: '/about', label: '关于' },
]
</script>

<template>
  <header class="sticky top-0 z-50 bg-rocom-nav-surface backdrop-blur-sm border-b border-rocom-outline">
    <div class="max-w-4xl mx-auto px-4 h-16 flex items-center justify-between">
      <NuxtLink to="/" class="text-xl font-bold text-rocom-text-strong tracking-wider">
        {{ siteConfig?.site_title || 'YeYi 的博客' }}
      </NuxtLink>

      <nav class="hidden md:flex items-center gap-6">
        <NuxtLink
          v-for="link in navLinks"
          :key="link.path"
          :to="link.path"
          class="text-sm font-medium text-rocom-text-secondary hover:text-rocom-primary transition-colors"
          :class="{ 'text-rocom-primary': route.path === link.path }"
        >
          {{ link.label }}
        </NuxtLink>
        <SearchBox />
        <DarkToggle />
      </nav>

      <button class="md:hidden text-rocom-text" @click="mobileMenuOpen = !mobileMenuOpen">
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path v-if="!mobileMenuOpen" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
          <path v-else stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
        </svg>
      </button>
    </div>

    <div v-if="mobileMenuOpen" class="md:hidden border-t border-rocom-outline bg-rocom-surface-paper">
      <nav class="flex flex-col p-4 gap-3">
        <NuxtLink
          v-for="link in navLinks"
          :key="link.path"
          :to="link.path"
          class="text-sm font-medium text-rocom-text-secondary hover:text-rocom-primary py-2"
          @click="mobileMenuOpen = false"
        >
          {{ link.label }}
        </NuxtLink>
        <div class="flex items-center gap-3 pt-2 border-t border-rocom-outline">
          <SearchBox />
          <DarkToggle />
        </div>
      </nav>
    </div>
  </header>
</template>
```

- [ ] **Step 3: 创建 AppFooter.vue**

```vue
<script setup lang="ts">
const { siteConfig } = useSiteConfig()
</script>

<template>
  <footer class="border-t border-rocom-outline bg-rocom-bg-warm py-8 text-center">
    <p class="text-sm text-rocom-text-caption">
      {{ siteConfig?.footer_text || '© 2026 YeYi' }}
    </p>
  </footer>
</template>
```

- [ ] **Step 4: 创建 DarkToggle.vue**

```vue
<script setup lang="ts">
const colorMode = useColorMode()
const toggle = () => {
  colorMode.preference = colorMode.value === 'dark' ? 'light' : 'dark'
}
</script>

<template>
  <button
    @click="toggle"
    class="w-9 h-9 rounded-full flex items-center justify-center bg-rocom-control hover:bg-rocom-control-hover transition-colors"
    :title="colorMode.value === 'dark' ? '切换亮色' : '切换暗色'"
  >
    <span v-if="colorMode.value === 'dark'" class="text-lg">☀️</span>
    <span v-else class="text-lg">🌙</span>
  </button>
</template>
```

- [ ] **Step 5: 创建 SearchBox.vue**

```vue
<script setup lang="ts">
const router = useRouter()
const query = ref('')

const doSearch = () => {
  if (query.value.trim()) {
    router.push({ path: '/search', query: { q: query.value.trim() } })
  }
}
</script>

<template>
  <form @submit.prevent="doSearch" class="relative">
    <input
      v-model="query"
      type="text"
      placeholder="搜索..."
      class="w-36 h-9 px-3 pr-8 text-sm rounded-pill bg-rocom-control border border-rocom-outline focus:outline-none focus:ring-2 focus:ring-rocom-primary/30 text-rocom-text placeholder:text-rocom-text-disabled"
    />
    <button type="submit" class="absolute right-2 top-1/2 -translate-y-1/2 text-rocom-text-muted hover:text-rocom-primary">
      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
      </svg>
    </button>
  </form>
</template>
```

- [ ] **Step 6: Commit**

```bash
cd frontend && git add -A && git commit -m "feat: layout, header, footer, dark toggle, search box"
```

---

## Task 4: 通用组件（PostCard、Pagination、公告栏）

**Files:**
- Create: `frontend/components/PostCard.vue`
- Create: `frontend/components/PostMeta.vue`
- Create: `frontend/components/Pagination.vue`
- Create: `frontend/components/AnnouncementBar.vue`
- Create: `frontend/components/BackToTop.vue`

**Interfaces:**
- Produces: 文章卡片、分页、公告栏、回到顶部等可复用组件

- [ ] **Step 1: 创建 PostMeta.vue**

```vue
<script setup lang="ts">
defineProps<{
  category?: { name: string; slug: string } | null
  tags?: { name: string; slug: string }[]
  publishedAt?: string | null
  viewCount?: number
}>()
</script>

<template>
  <div class="flex flex-wrap items-center gap-2 text-xs text-rocom-text-caption">
    <time v-if="publishedAt" :datetime="publishedAt">
      {{ new Date(publishedAt).toLocaleDateString('zh-CN') }}
    </time>
    <span v-if="category" class="px-2 py-0.5 rounded-pill bg-rocom-primary-soft text-rocom-primary-outline font-medium">
      {{ category.name }}
    </span>
    <span v-for="tag in tags" :key="tag.slug" class="px-2 py-0.5 rounded-pill bg-rocom-control text-rocom-text-muted">
      {{ tag.name }}
    </span>
    <span v-if="viewCount" class="text-rocom-text-disabled">
      {{ viewCount }} 阅读
    </span>
  </div>
</template>
```

- [ ] **Step 2: 创建 PostCard.vue**

```vue
<script setup lang="ts">
defineProps<{
  post: {
    title: string
    slug: string
    excerpt?: string | null
    cover_image?: string | null
    category?: { name: string; slug: string } | null
    tags?: { name: string; slug: string }[]
    published_at?: string | null
    view_count?: number
  }
}>()
</script>

<template>
  <article class="group bg-rocom-surface-paper rounded-2xl border border-rocom-outline shadow-paper hover:shadow-lg hover:-translate-y-0.5 transition-all duration-200 overflow-hidden">
    <NuxtLink :to="`/posts/${post.slug}`" class="block">
      <img v-if="post.cover_image" :src="post.cover_image" :alt="post.title" class="w-full h-48 object-cover" />
      <div class="p-5">
        <h2 class="text-lg font-bold text-rocom-text-strong group-hover:text-rocom-primary transition-colors mb-2 line-clamp-2">
          {{ post.title }}
        </h2>
        <p v-if="post.excerpt" class="text-sm text-rocom-text-secondary line-clamp-3 mb-3">
          {{ post.excerpt }}
        </p>
        <PostMeta
          :category="post.category"
          :tags="post.tags"
          :published-at="post.published_at"
          :view-count="post.view_count"
        />
      </div>
    </NuxtLink>
  </article>
</template>
```

- [ ] **Step 3: 创建 Pagination.vue**

```vue
<script setup lang="ts">
const props = defineProps<{
  page: number
  totalPages: number
}>()
const emit = defineEmits<{ (e: 'change', page: number): void }>()

const pages = computed(() => {
  const p: number[] = []
  const start = Math.max(1, props.page - 2)
  const end = Math.min(props.totalPages, props.page + 2)
  for (let i = start; i <= end; i++) p.push(i)
  return p
})
</script>

<template>
  <nav v-if="totalPages > 1" class="flex items-center justify-center gap-1 mt-8">
    <button
      :disabled="page <= 1"
      class="px-3 py-1.5 text-sm rounded-lg bg-rocom-control hover:bg-rocom-control-hover disabled:opacity-40 text-rocom-text"
      @click="emit('change', page - 1)"
    >
      上一页
    </button>
    <button
      v-for="p in pages"
      :key="p"
      class="w-9 h-9 text-sm rounded-lg transition-colors"
      :class="p === page ? 'bg-rocom-primary text-rocom-text-strong font-bold' : 'bg-rocom-control hover:bg-rocom-control-hover text-rocom-text'"
      @click="emit('change', p)"
    >
      {{ p }}
    </button>
    <button
      :disabled="page >= totalPages"
      class="px-3 py-1.5 text-sm rounded-lg bg-rocom-control hover:bg-rocom-control-hover disabled:opacity-40 text-rocom-text"
      @click="emit('change', page + 1)"
    >
      下一页
    </button>
  </nav>
</template>
```

- [ ] **Step 4: 创建 AnnouncementBar.vue**

```vue
<script setup lang="ts">
defineProps<{ content: string }>()
</script>

<template>
  <div v-if="content" class="bg-rocom-primary-soft border border-rocom-primary/20 rounded-xl px-4 py-3 text-sm text-rocom-text-strong mb-6">
    {{ content }}
  </div>
</template>
```

- [ ] **Step 5: 创建 BackToTop.vue**

```vue
<script setup lang="ts">
const visible = ref(false)
const check = () => { visible.value = window.scrollY > 300 }
const goTop = () => window.scrollTo({ top: 0, behavior: 'smooth' })

onMounted(() => window.addEventListener('scroll', check))
onUnmounted(() => window.removeEventListener('scroll', check))
</script>

<template>
  <Transition name="fade">
    <button
      v-if="visible"
      @click="goTop"
      class="fixed bottom-6 right-6 w-10 h-10 rounded-full bg-rocom-primary text-rocom-text-strong shadow-lg flex items-center justify-center hover:bg-rocom-primary-strong transition-colors z-50"
    >
      ↑
    </button>
  </Transition>
</template>

<style scoped>
.fade-enter-active, .fade-leave-active { transition: opacity 0.2s; }
.fade-enter-from, .fade-leave-to { opacity: 0; }
</style>
```

- [ ] **Step 6: Commit**

```bash
cd frontend && git add -A && git commit -m "feat: PostCard, Pagination, AnnouncementBar, BackToTop components"
```

---

## Task 5: 首页

**Files:**
- Create: `frontend/pages/index.vue`

**Interfaces:**
- Consumes: `GET /api/v1/posts`, `GET /api/v1/site/announcement`
- Produces: 首页（公告栏 + 文章卡片网格 + 分页）

- [ ] **Step 1: 创建 index.vue**

```vue
<script setup lang="ts">
const api = useApi()
const route = useRoute()
const router = useRouter()

const page = computed(() => Number(route.query.page) || 1)

const { data: postsData } = await useAsyncData(
  `posts-${page.value}`,
  () => api.get('/api/v1/posts', { page: page.value, page_size: 10 })
)

const { data: announcement } = await useAsyncData(
  'announcement',
  () => api.get('/api/v1/site/announcement')
)

const totalPages = computed(() =>
  postsData.value ? Math.ceil(postsData.value.total / postsData.value.page_size) : 1
)

const goToPage = (p: number) => {
  router.push({ query: { ...route.query, page: p > 1 ? p : undefined } })
}
</script>

<template>
  <div>
    <AnnouncementBar v-if="announcement?.content" :content="announcement.content" />

    <div class="grid gap-6 sm:grid-cols-2">
      <PostCard v-for="post in postsData?.items" :key="post.id" :post="post" />
    </div>

    <div v-if="postsData?.items?.length === 0" class="text-center py-16 text-rocom-text-muted">
      暂无文章
    </div>

    <Pagination :page="page" :total-pages="totalPages" @change="goToPage" />
  </div>
</template>
```

- [ ] **Step 2: Commit**

```bash
cd frontend && git add -A && git commit -m "feat: homepage with post list, announcement, pagination"
```

---

## Task 6: 文章详情页

**Files:**
- Create: `frontend/pages/posts/[slug].vue`
- Create: `frontend/assets/css/typography.css`
- Create: `frontend/components/TableOfContents.vue`

**Interfaces:**
- Consumes: `GET /api/v1/posts/{slug}`
- Produces: 文章详情页（标题、元信息、正文、目录）

- [ ] **Step 1: 创建 typography.css**

```css
.prose {
  line-height: 1.8;
  font-size: 1.125rem;
  color: var(--rocom-text);
}

.prose h1, .prose h2, .prose h3, .prose h4 {
  color: var(--rocom-text-strong);
  font-weight: 700;
  margin-top: 2em;
  margin-bottom: 0.5em;
}

.prose h1 { font-size: 2rem; }
.prose h2 { font-size: 1.5rem; border-bottom: 1px solid var(--rocom-outline); padding-bottom: 0.3em; }
.prose h3 { font-size: 1.25rem; }

.prose p { margin-bottom: 1em; }

.prose a {
  color: var(--rocom-accent-blue);
  text-decoration: underline;
  text-underline-offset: 2px;
}

.prose img {
  max-width: 100%;
  border-radius: 12px;
  margin: 1.5em 0;
}

.prose blockquote {
  border-left: 4px solid var(--rocom-primary);
  padding-left: 1em;
  color: var(--rocom-text-secondary);
  margin: 1.5em 0;
}

.prose code {
  background: var(--rocom-bg-parchment);
  padding: 0.2em 0.4em;
  border-radius: 4px;
  font-size: 0.875em;
  font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
}

.prose pre {
  background: #2D2A24;
  color: #F4EEE1;
  padding: 1em 1.5em;
  border-radius: 12px;
  overflow-x: auto;
  margin: 1.5em 0;
  font-size: 0.875em;
  line-height: 1.6;
}

.prose pre code {
  background: none;
  padding: 0;
  color: inherit;
}

.prose ul, .prose ol {
  padding-left: 1.5em;
  margin-bottom: 1em;
}

.prose li { margin-bottom: 0.25em; }

.prose table {
  width: 100%;
  border-collapse: collapse;
  margin: 1.5em 0;
}

.prose th, .prose td {
  border: 1px solid var(--rocom-outline);
  padding: 0.5em 0.75em;
  text-align: left;
}

.prose th {
  background: var(--rocom-bg-paper);
  font-weight: 600;
}

.prose hr {
  border: none;
  border-top: 1px solid var(--rocom-separator);
  margin: 2em 0;
}
```

- [ ] **Step 2: 创建 TableOfContents.vue**

```vue
<script setup lang="ts">
interface TocItem { id: string; text: string; level: number }
defineProps<{ items: TocItem[] }>()
</script>

<template>
  <nav v-if="items.length" class="hidden lg:block sticky top-24 w-56 shrink-0">
    <h4 class="text-xs font-bold text-rocom-text-caption uppercase tracking-widest mb-3">目录</h4>
    <ul class="space-y-1.5 text-sm">
      <li v-for="item in items" :key="item.id">
        <a
          :href="`#${item.id}`"
          class="block text-rocom-text-muted hover:text-rocom-primary transition-colors"
          :style="{ paddingLeft: `${(item.level - 2) * 0.75}rem` }"
        >
          {{ item.text }}
        </a>
      </li>
    </ul>
  </nav>
</template>
```

- [ ] **Step 3: 创建 [slug].vue**

```vue
<script setup lang="ts">
const api = useApi()
const route = useRoute()
const slug = route.params.slug as string

const { data: post } = await useAsyncData(
  `post-${slug}`,
  () => api.get(`/api/v1/posts/${slug}`)
)

if (!post.value) {
  throw createError({ statusCode: 404, message: '文章不存在' })
}

useHead({ title: post.value.title })

const tocItems = computed(() => {
  if (!post.value?.content_html) return []
  const regex = /<h([2-4])\s+id="([^"]*)"[^>]*>(.*?)<\/h[2-4]>/g
  const items: { id: string; text: string; level: number }[] = []
  let match
  while ((match = regex.exec(post.value.content_html)) !== null) {
    items.push({ level: Number(match[1]), id: match[2], text: match[3].replace(/<[^>]+>/g, '') })
  }
  return items
})
</script>

<template>
  <article v-if="post" class="flex gap-8">
    <div class="flex-1 min-w-0">
      <header class="mb-8">
        <h1 class="text-3xl font-bold text-rocom-text-strong mb-4">{{ post.title }}</h1>
        <PostMeta
          :category="post.category"
          :tags="post.tags"
          :published-at="post.published_at"
          :view-count="post.view_count"
        />
      </header>

      <div class="prose" v-html="post.content_html" />

      <CommentSection :post-slug="slug" />
    </div>

    <TableOfContents :items="tocItems" />
  </article>
</template>
```

- [ ] **Step 4: 在 main.css 中引入 typography**

在 `assets/css/main.css` 末尾添加：
```css
@import './typography.css';
```

- [ ] **Step 5: Commit**

```bash
cd frontend && git add -A && git commit -m "feat: article detail page with TOC and typography"
```

---

## Task 7: 评论组件

**Files:**
- Create: `frontend/components/CommentSection.vue`
- Create: `frontend/components/CommentItem.vue`
- Create: `frontend/components/CommentForm.vue`

**Interfaces:**
- Consumes: `GET /api/v1/posts/{slug}/comments`, `POST /api/v1/comments`
- Produces: 评论区（评论列表 + 嵌套回复 + 评论表单）

- [ ] **Step 1: 创建 CommentItem.vue**

```vue
<script setup lang="ts">
defineProps<{
  comment: {
    id: number
    nickname: string
    website?: string | null
    content: string
    created_at: string
    replies?: any[]
  }
  depth?: number
}>()

const formatDate = (d: string) => new Date(d).toLocaleDateString('zh-CN')
</script>

<template>
  <div class="py-4" :class="{ 'ml-8 border-l-2 border-rocom-outline pl-4': depth }">
    <div class="flex items-center gap-2 mb-2">
      <span class="font-semibold text-sm text-rocom-text-strong">{{ comment.nickname }}</span>
      <span class="text-xs text-rocom-text-caption">{{ formatDate(comment.created_at) }}</span>
    </div>
    <p class="text-sm text-rocom-text-secondary whitespace-pre-wrap">{{ comment.content }}</p>
    <CommentItem
      v-for="reply in comment.replies"
      :key="reply.id"
      :comment="reply"
      :depth="(depth || 0) + 1"
    />
  </div>
</template>
```

- [ ] **Step 2: 创建 CommentForm.vue**

```vue
<script setup lang="ts">
const props = defineProps<{ postSlug: string }>()
const emit = defineEmits<{ (e: 'submitted'): void }>()
const api = useApi()

const form = reactive({
  nickname: '',
  email: '',
  website: '',
  content: '',
})
const submitting = ref(false)
const submitted = ref(false)

const submit = async () => {
  if (!form.nickname.trim() || !form.content.trim()) return
  submitting.value = true
  try {
    await api.post('/api/v1/comments', {
      post_slug: props.postSlug,
      nickname: form.nickname,
      email: form.email || undefined,
      website: form.website || undefined,
      content: form.content,
    })
    submitted.value = true
    form.content = ''
    emit('submitted')
  } finally {
    submitting.value = false
  }
}
</script>

<template>
  <div class="bg-rocom-surface-paper rounded-xl border border-rocom-outline p-5">
    <h3 class="text-sm font-bold text-rocom-text-strong mb-4">发表评论</h3>
    <div v-if="submitted" class="text-sm text-rocom-success py-4 text-center">
      评论已提交，等待审核后显示。
    </div>
    <form v-else @submit.prevent="submit" class="space-y-3">
      <div class="grid grid-cols-1 sm:grid-cols-3 gap-3">
        <input v-model="form.nickname" placeholder="昵称 *" required class="input-field" />
        <input v-model="form.email" placeholder="邮箱（可选）" type="email" class="input-field" />
        <input v-model="form.website" placeholder="网站（可选）" class="input-field" />
      </div>
      <textarea v-model="form.content" placeholder="写下你的评论..." required rows="4" class="input-field w-full" />
      <button
        type="submit"
        :disabled="submitting"
        class="px-5 py-2 rounded-pill bg-rocom-primary text-rocom-text-strong font-medium text-sm hover:bg-rocom-primary-strong disabled:opacity-50 transition-colors"
      >
        {{ submitting ? '提交中...' : '提交评论' }}
      </button>
    </form>
  </div>
</template>

<style scoped>
.input-field {
  @apply px-3 py-2 text-sm rounded-lg bg-rocom-control border border-rocom-outline focus:outline-none focus:ring-2 focus:ring-rocom-primary/30 text-rocom-text placeholder:text-rocom-text-disabled;
}
</style>
```

- [ ] **Step 3: 创建 CommentSection.vue**

```vue
<script setup lang="ts">
const props = defineProps<{ postSlug: string }>()
const api = useApi()

const { data: comments, refresh } = await useAsyncData(
  `comments-${props.postSlug}`,
  () => api.get(`/api/v1/posts/${props.postSlug}/comments`)
)
</script>

<template>
  <section class="mt-12 border-t border-rocom-outline pt-8">
    <h2 class="text-lg font-bold text-rocom-text-strong mb-6">评论</h2>

    <div v-if="comments?.length" class="divide-y divide-rocom-outline">
      <CommentItem v-for="c in comments" :key="c.id" :comment="c" />
    </div>
    <p v-else class="text-sm text-rocom-text-muted py-4">暂无评论，来抢沙发吧！</p>

    <div class="mt-8">
      <CommentForm :post-slug="postSlug" @submitted="refresh()" />
    </div>
  </section>
</template>
```

- [ ] **Step 4: Commit**

```bash
cd frontend && git add -A && git commit -m "feat: comment section with nested replies and form"
```

---

## Task 8: 分类、标签、归档、搜索、关于页

**Files:**
- Create: `frontend/pages/categories.vue`
- Create: `frontend/pages/tags.vue`
- Create: `frontend/pages/archive.vue`
- Create: `frontend/pages/search.vue`
- Create: `frontend/pages/about.vue`
- Create: `frontend/components/CategoryList.vue`
- Create: `frontend/components/TagCloud.vue`

**Interfaces:**
- Consumes: `GET /api/v1/categories`, `GET /api/v1/tags`, `GET /api/v1/search`, `GET /api/v1/posts`
- Produces: 5 个页面

- [ ] **Step 1: 创建 CategoryList.vue**

```vue
<script setup lang="ts">
defineProps<{ categories: { id: number; name: string; slug: string; description?: string | null }[] }>()
</script>

<template>
  <div class="grid gap-4 sm:grid-cols-2">
    <NuxtLink
      v-for="cat in categories"
      :key="cat.id"
      :to="{ path: '/', query: { category: cat.slug } }"
      class="bg-rocom-surface-paper rounded-xl border border-rocom-outline p-4 hover:shadow-paper hover:-translate-y-0.5 transition-all"
    >
      <h3 class="font-bold text-rocom-text-strong">{{ cat.name }}</h3>
      <p v-if="cat.description" class="text-sm text-rocom-text-secondary mt-1">{{ cat.description }}</p>
    </NuxtLink>
  </div>
</template>
```

- [ ] **Step 2: 创建 TagCloud.vue**

```vue
<script setup lang="ts">
defineProps<{ tags: { id: number; name: string; slug: string }[] }>()
</script>

<template>
  <div class="flex flex-wrap gap-2">
    <NuxtLink
      v-for="tag in tags"
      :key="tag.id"
      :to="{ path: '/', query: { tag: tag.slug } }"
      class="px-3 py-1.5 rounded-pill bg-rocom-control text-sm text-rocom-text-secondary hover:bg-rocom-primary-soft hover:text-rocom-primary-outline transition-colors"
    >
      {{ tag.name }}
    </NuxtLink>
  </div>
</template>
```

- [ ] **Step 3: 创建 categories.vue**

```vue
<script setup lang="ts">
const api = useApi()
useHead({ title: '分类' })
const { data: categories } = await useAsyncData('categories', () => api.get('/api/v1/categories'))
</script>

<template>
  <div>
    <h1 class="text-2xl font-bold text-rocom-text-strong mb-6">分类</h1>
    <CategoryList v-if="categories?.length" :categories="categories" />
    <p v-else class="text-rocom-text-muted">暂无分类</p>
  </div>
</template>
```

- [ ] **Step 4: 创建 tags.vue**

```vue
<script setup lang="ts">
const api = useApi()
useHead({ title: '标签' })
const { data: tags } = await useAsyncData('tags', () => api.get('/api/v1/tags'))
</script>

<template>
  <div>
    <h1 class="text-2xl font-bold text-rocom-text-strong mb-6">标签</h1>
    <TagCloud v-if="tags?.length" :tags="tags" />
    <p v-else class="text-rocom-text-muted">暂无标签</p>
  </div>
</template>
```

- [ ] **Step 5: 创建 archive.vue**

```vue
<script setup lang="ts">
const api = useApi()
useHead({ title: '归档' })

const { data: postsData } = await useAsyncData('archive', () =>
  api.get('/api/v1/posts', { page_size: 100 })
)

const grouped = computed(() => {
  if (!postsData.value?.items) return {}
  const groups: Record<string, any[]> = {}
  for (const post of postsData.value.items) {
    const year = new Date(post.published_at || post.created_at).getFullYear().toString()
    if (!groups[year]) groups[year] = []
    groups[year].push(post)
  }
  return groups
})
</script>

<template>
  <div>
    <h1 class="text-2xl font-bold text-rocom-text-strong mb-6">归档</h1>
    <div v-for="(posts, year) in grouped" :key="year" class="mb-8">
      <h2 class="text-lg font-bold text-rocom-primary mb-4">{{ year }}</h2>
      <ul class="space-y-2">
        <li v-for="post in posts" :key="post.id" class="flex items-baseline gap-3">
          <span class="text-xs text-rocom-text-caption shrink-0 w-16">
            {{ new Date(post.published_at || post.created_at).toLocaleDateString('zh-CN', { month: '2-digit', day: '2-digit' }) }}
          </span>
          <NuxtLink :to="`/posts/${post.slug}`" class="text-rocom-text hover:text-rocom-primary transition-colors">
            {{ post.title }}
          </NuxtLink>
        </li>
      </ul>
    </div>
  </div>
</template>
```

- [ ] **Step 6: 创建 search.vue**

```vue
<script setup lang="ts">
const api = useApi()
const route = useRoute()
const router = useRouter()
const query = computed(() => (route.query.q as string) || '')
const page = computed(() => Number(route.query.page) || 1)

useHead({ title: computed(() => query.value ? `搜索: ${query.value}` : '搜索') })

const { data: results } = await useAsyncData(
  `search-${query.value}-${page.value}`,
  () => query.value ? api.get('/api/v1/search', { q: query.value, page: page.value }) : null,
  { watch: [query, page] }
)

const totalPages = computed(() =>
  results.value ? Math.ceil(results.value.total / results.value.page_size) : 1
)

const goToPage = (p: number) => {
  router.push({ query: { q: query.value, page: p > 1 ? p : undefined } })
}
</script>

<template>
  <div>
    <h1 class="text-2xl font-bold text-rocom-text-strong mb-6">
      搜索: <span class="text-rocom-primary">{{ query }}</span>
    </h1>

    <div v-if="results?.items?.length" class="grid gap-6 sm:grid-cols-2">
      <PostCard v-for="post in results.items" :key="post.id" :post="post" />
    </div>
    <p v-else-if="query" class="text-rocom-text-muted py-8 text-center">未找到相关文章</p>

    <Pagination :page="page" :total-pages="totalPages" @change="goToPage" />
  </div>
</template>
```

- [ ] **Step 7: 创建 about.vue**

```vue
<script setup lang="ts">
useHead({ title: '关于' })
</script>

<template>
  <div class="prose max-w-none">
    <h1>关于</h1>
    <p>这是 YeYi 的个人博客，记录生活与代码。</p>
    <p>使用 Nuxt 3 + FastAPI 构建，采用洛克魔法书设计风格。</p>
  </div>
</template>
```

- [ ] **Step 8: Commit**

```bash
cd frontend && git add -A && git commit -m "feat: categories, tags, archive, search, about pages"
```

---

## Task 9: 暗黑模式适配与最终样式调整

**Files:**
- Modify: `frontend/assets/css/main.css` — 确保暗黑模式变量完整
- Modify: `frontend/assets/css/typography.css` — 暗黑模式下的代码块样式

- [ ] **Step 1: 确认暗黑模式**

- 检查所有组件在 dark mode 下的显示
- 代码块在暗黑模式下保持深色背景
- 确认 color-mode 切换正常工作

- [ ] **Step 2: 在 tailwind.config.ts 中确认 darkMode: 'class'**

已配置。

- [ ] **Step 3: 验证响应式**

- 移动端：导航折叠为汉堡菜单
- 平板：文章卡片单列/双列
- 桌面：文章详情右侧显示 TOC

- [ ] **Step 4: Commit**

```bash
cd frontend && git add -A && git commit -m "fix: dark mode and responsive style adjustments"
```

---

## Task 10: Nuxt 配置完善与生产构建

**Files:**
- Modify: `frontend/nuxt.config.ts`

- [ ] **Step 1: 完善 nuxt.config.ts**

确认以下配置：
- SSR 模式默认开启
- API base URL 通过环境变量配置
- SEO meta tags
- 字体预加载

- [ ] **Step 2: 验证生产构建**

Run: `cd frontend && npm run build`
Expected: 构建成功

- [ ] **Step 3: Commit**

```bash
cd frontend && git add -A && git commit -m "chore: nuxt config polish and production build verification"
```
